{
  "name": "fnode",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vitest bench",
    "build": "tsc --build",
    "typecheck": "tsc --build --noEmit",
    "test:run": "bash scripts/run-tests.sh",
    "bench:run": "bash scripts/run-bench.sh",
    "test:report": "bun run scripts/generate-report.ts test",
    "bench:report": "bun run scripts/generate-report.ts bench",
    "report": "bun run test:run && bun run bench:run && bun run test:report && bun run bench:report"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/react-dom": "^19.2.3",
    "@vitest/coverage-v8": "^4.0.18",
    "bun-types": "^1.3.10",
    "happy-dom": "^20.7.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
